package game;

/**
 * enumeration holding the render depths of each kind of object. the ordinal
 *   values of the enumerations are used to determine the render dpeths.
 */
public enum RenderDepths
{
    WALL,
    GUNNER,
    BULLET
}
